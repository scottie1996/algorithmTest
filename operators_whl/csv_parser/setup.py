from setuptools import setup, find_packages

setup(
    name='csv-processor',
    version='0.1.0',
    packages=find_packages(),
    description='CSV file handler for operator framework',
    author='Your Name',
    install_requires=[],  # 可添加如 numpy 等依赖
    python_requires='>=3.6',
)
